# Media assets for documentation

This repository does not ship photos/screenshots yet. Add your own images here and keep filenames stable so `README.md` renders nicely.

## Suggested structure
- `docs/media/hardware/`
  - `jimu-master-brick.svg` (placeholder in repo; replace with your photo)
  - `jimu-modules.svg` (placeholder in repo; replace with your photo)
  - `jimu-model.svg` (placeholder in repo; replace with your photo)
- `docs/media/screenshots/`
  - `project-home.svg` (placeholder in repo; replace with your screenshot)
  - `model-config.svg` (placeholder in repo; replace with your screenshot)
  - `blockly.svg` (placeholder in repo; replace with your screenshot)
  - `control-panel.svg` (placeholder in repo; replace with your screenshot)
